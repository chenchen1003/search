# Large local embedding weights (keep path as convention; do not commit binaries)
models/*.gguf
# ChromaDB vector store (project-local default; override with CHROMA_DIR env var)
chroma/
# Domain wiki and embedding cache (generated locally, editable)
domain-data/domain_emb.json
.venv/
__pycache__/
*.pyc
*.pyo
.env
*.egg-info/
dist/
build/
.pytest_cache/
