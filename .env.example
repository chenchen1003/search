CHROMA_DIR=~/.local/share/knowledge/chroma
# Default: project-local GGUF (path is resolved vs. the ``search`` repo root).
EMBED_MODEL=models/Qwen3-Embedding-0.6B-Q8_0.gguf
# HuggingFace SentenceTransformer instead (needs HF download + no GGUF path):
# EMBED_MODEL=Qwen/Qwen3-Embedding-0.6B
CHUNK_SIZE=400
CHUNK_OVERLAP=50
API_PORT=8000
